# 全栈学习流程（国内通用 · 高频优先）

> **目标：** 具备国内常见 Java 全栈交付能力（库表 → 接口 → 鉴权 → 联调），不是把本仓库功能做完。  
> **本仓库：** 可选对照/练手。  
> **每课：** ①是什么 → ②为什么 → ③细节 → ④**练习** → ⑤复盘 → ⑥面试短答（仅高频）→ ⑦工作场景（仅常见）。  
> **原则：** **常用讲透+多练**；不常用 **～带过** 或进不学清单。几乎零基础。前端对照可用。

进度把 `[ ]` 改成 `[x]`。  
**C01～C30 已过**；当前 **P 补全从 P0.1**（★必练优先）。

---

## 能力地图

```text
日常主线（★必练，多练）：
  起服务联调 → 写接口(分层/R/DTO/校验) → SQL/MP/分页
  → Token鉴权 → 前后端契约/CORS → 排错

微服务辅线（·会用即可）：
  Nacos注册 · 网关路由 · Feign调用

带过/后置（～）：
  Optional深挖 · 集合八股 · 配置中心深配 · Sentinel深配 · JWT辩论 · 幂等深挖
```

岗位高频栈：`Java 17/21` · **Boot3** · **SCA** · `MySQL` · `Redis` · `MyBatis-Plus` · React/Vue+Antd。

---

## 总览

| 阶段 | 课 | 主题 | 状态 |
|------|----|------|------|
| **J～F** | C01～C30 | 认路（已过） | [x] |
| **P 补全** | P0～P7 | ★必练为主 + 练习 + 高频面试/场景 | 进行中 |

图例：**★必练**（讲透+练习）· **·常考**（短答即可）· **～带过**（一句话认知，不展开）

C 课细则在文后供回看；**带学以 P 为准**。

---

## 系统知识补全（高频精修版）

### 带学约定

| 项 | 做法 |
|----|------|
| 时间分配 | ★ 占 80%；· 面试口述；～ 最多 1～2 句 |
| 每小课 | ①～⑦，其中 **④练习必做**；⑥⑦只保留本块「高频」表里的题/场景 |
| 联系 | 每块有「和前后块的关系」，避免孤立背概念 |
| 卡住 | 直接给答案+为什么；不展开～项 |

### 块优先级

| 块 | 优先级 | 过关 | 状态 |
|----|--------|------|------|
| **P0** | ★ | 联调走通 + 三种错能定位 | [ ] |
| **P1** | ★（精简） | 看懂 Service；`equals`/List/异常 | [ ] |
| **P2** | ★ | 条件查询+分页；Flyway 加列 | [ ] |
| **P3** | ★核心 | 独立交付小接口 | [ ] |
| **P4** | ★ | Token 全路径；401/403 | [ ] |
| **P5** | · | 注册/路由/Feign 会查；配置中心与 Sentinel～ | [ ] |
| **P6** | ★ | 双码+CORS+字段对齐 | [ ] |
| **P7** | ★ | 5 分钟口述 + 排障顺序 | [ ] |

---

### P0 环境与请求链路 ★

**和后面的联系：** 所有写接口（P3）、鉴权（P4）、联调（P6）都建立在「请求怎么进系统」上。先练这条肌肉。

| 小课 | 内容 | 标 | 状态 |
|------|------|----|------|
| **P0.1** | 进程分工：Nacos / Redis / gateway / user（+biz）；启动顺序 | ★ | [ ] |
| **P0.2** | yml：`port`、`spring.application.name`、Nacos/Redis 地址 | ★ | [ ] |
| **P0.3** | 路由：`/api/**`、`lb://`、`StripPrefix` | ★ | [ ] |
| **P0.4** | Apifox 登录：URL/Method/Body；取出 token | ★ | [ ] |
| **P0.5** | 带 `X-Token` 调业务；Cookie 方案只～对比一句 | ★ | [ ] |
| **P0.6** | 排错练习：无 Token / 错路径 / 停 user | ★ | [ ] |

**练习（必做）**

1. 按顺序起：Nacos → Redis → user → gateway（biz 可选）  
2. `POST /api/user/login` 拿到 token  
3. 带 `X-Token` 调一个需登录接口  
4. 再各做错一次：不带 Token、路径少 `/api`、停掉 user——记下现象  

**面试 ·常考**

| 题 | 短答方向 |
|----|----------|
| 为什么要网关？ | 统一入口、鉴权、路由、CORS |
| 注册发现解决什么？ | 不写死 IP，按服务名找实例 |
| 联调为何打网关？ | 和线上一致 |

～带过：微服务是否一定优于单体（答「看边界与成本」即可）。

**工作场景 ★**

| 场景 | 先做啥 |
|------|--------|
| 群里说接口不通 | 问 URL、是否网关、Token、HTTP 与 body.code |
| 直连 8081 通、前端不通 | CORS / 是否 `/api` / 鉴权头 |
| 找不到服务 | Nacos 是否在、实例是否注册 |

**过关：** 练习 1～4 做过 + 链路口述 + 上表 2 题。

---

### P1 Java 常用细节 ★（砍掉八股）

**和后面的联系：** Service/Controller 里满是 List、String、异常；看不懂就写不了 P3。

| 小课 | 内容 | 标 | 状态 |
|------|------|----|------|
| **P1.1** | class / method / 包名=目录 | ★ | [ ] |
| **P1.2** | 基本类型 vs 包装类型；`null`；对照 TS | ★ | [ ] |
| **P1.3** | `String` 空判断；**`==` vs `equals`** | ★ | [ ] |
| **P1.4** | `List` 增删遍历；接口返回列表 | ★ | [ ] |
| **P1.5** | `Map` 按 id 取；组装返回 | ★ | [ ] |
| **P1.6** | `throw BizException`；别乱吞异常 | ★ | [ ] |
| **P1.7** | Lombok `@Data`、构造器注入（对照仓库） | ★ | [ ] |
| **P1.x** | Optional / stream / LinkedList 对比 | ～ | 带过 |

**练习（必做）**

1. 打开 `UserService`（或任意 Service），逐行用自己的话注释「这行在干嘛」  
2. 指出一处 `List` 或 `equals`/`BizException`  

**面试 ·常考**

| 题 | 短答方向 |
|----|----------|
| `==` 和 `equals`？ | 字符串比内容用 equals |
| 什么是 NPE？ | 空指针；先查哪行为 null |

～带过：ArrayList vs LinkedList、受检/非受检长辨析。

**工作场景 ★**

| 场景 | 先做啥 |
|------|--------|
| 日志 NPE | 堆栈第一处业务行，谁为 null |
| 返回缺字段 | 是否异常被处理、DTO 是否没赋值 |

**过关：** 练习完成 + 上表 2 题短答。

---

### P2 SQL · MP · 分页 · Flyway ★

**和后面的联系：** P3 写列表/详情几乎都是「条件 + 分页 + 实体映射」。

| 小课 | 内容 | 标 | 状态 |
|------|------|----|------|
| **P2.1** | 表/主键；实体字段对应 | ★ | [ ] |
| **P2.2** | SELECT/WHERE/LIKE/ORDER BY | ★ | [ ] |
| **P2.3** | 索引：加速 WHERE；不是越多越好 | · | [ ] |
| **P2.4** | `@Transactional`：多步写库同成同败 | · | [ ] |
| **P2.5** | 实体 + `BaseMapper`；MP 少写 SQL | ★ | [ ] |
| **P2.6** | `LambdaQueryWrapper`；空条件别误伤 | ★ | [ ] |
| **P2.7** | 分页：页码、每页条数、records/total | ★ | [ ] |
| **P2.8** | Flyway：只新增 `Vn__`；不改旧脚本 | ★ | [ ] |

**练习（必做）**

1. 经网关调一个列表/搜索（或自写条件查询）看 SQL/返回  
2. 对照已有分页接口，说出 page / size 从哪来、total 是什么  
3. 新增一个 Flyway 脚本加一列（可随后在 P3 用）  

**面试 ·常考**

| 题 | 短答方向 |
|----|----------|
| 索引优缺点？ | 加快查、影响写、占空间 |
| 事务是什么？ | 多步同成同败 |
| 为何用 Flyway？ | 表结构变更可重复、环境一致 |

～带过：事务失效全部原因、最左前缀、深分页优化算法。

**工作场景 ★**

| 场景 | 先做啥 |
|------|--------|
| 列表慢 | 看 SQL、WHERE 有无索引、一次是否过多 |
| 本地改表别人跑不起来 | 必须走迁移脚本 |
| 分页 total 不对 | list 与 count 条件是否一致 |

**过关：** 条件+分页能讲清 + Flyway 加列成功 + 面试 1 题。

---

### P3 分层写接口 ★核心

**和后面的联系：** 这是日常写需求的主技能；P4 鉴权、P6 契约都挂在你写出的接口上。

| 小课 | 内容 | 标 | 状态 |
|------|------|----|------|
| **P3.1** | Controller / Service / Mapper 职责 | ★ | [ ] |
| **P3.2** | 统一 `R`（code/message/data）与双码 | ★ | [ ] |
| **P3.3** | DTO vs 实体；不返回密码等字段 | ★ | [ ] |
| **P3.4** | `@Valid` + 全局异常 | ★ | [ ] |
| **P3.5** | REST：GET 查 / POST 写；路径习惯 | ★ | [ ] |
| **P3.6** | **实战练习：独立做一个小接口** | ★ | [ ] |

**练习（必做）**

1. 对照仓库画三层：某个接口进到哪几个类  
2. 故意触发校验失败，看返回长什么样  
3. **P3.6**：自选极小需求（如备注字段、简单查询），走完：DTO→校验→Service→Mapper→经网关调通  

**面试 ·常考**

| 题 | 短答方向 |
|----|----------|
| 为什么分层？ | 职责清晰，Controller 不堆业务 |
| 为什么用 DTO？ | 防泄露、解耦表结构 |
| 全局异常干什么？ | 统一错误 JSON |

～带过：幂等深度、GET 是否允许 body 长辩论（一句：查询别用 body）。

**工作场景 ★**

| 场景 | 先做啥 |
|------|--------|
| 前端嫌字段乱 | 专用 VO，只返回需要的 |
| 校验失败喷堆栈 | 查全局异常是否生效 |
| 要加字段 | 改 DTO + 更新 Apifox/文档 |

**过关：** P3.6 接口经网关成功 + 能讲每层文件。

---

### P4 Token · Redis · 鉴权 ★

**和后面的联系：** P0 联调、P6 前端 401，根因多半在这。

| 小课 | 内容 | 标 | 状态 |
|------|------|----|------|
| **P4.1** | 登录发 token；会话在 Redis | ★ | [ ] |
| **P4.2** | Key 前缀、TTL；多实例共享 | ★ | [ ] |
| **P4.3** | BCrypt；禁止明文 | ★ | [ ] |
| **P4.4** | 网关：白名单 → Token → Redis → 用户头 | ★ | [ ] |
| **P4.5** | 不信客户端伪造的 userId | ★ | [ ] |
| **P4.6** | 401 vs 403；写接口要角色（对照仓库） | ★ | [ ] |
| **P4.7** | **练习：** 改白名单或权限并自测 | ★ | [ ] |

**练习（必做）**

1. 登录后在 Redis 里找到对应 key（或看代码确认 key 规则）  
2. 无 Token / 错 Token / 无权限 各打一次，区分 401 与 403  
3. P4.7：改一处规则并恢复或留下学习向改动  

**面试 ·常考**

| 题 | 短答方向 |
|----|----------|
| 密码为何哈希？ | 防库泄露直接可读 |
| 401 和 403？ | 没登录 vs 没权限 |
| 网关鉴权好处？ | 业务少重复、统一入口 |

～带过：JWT vs Redis 长对比、Session 集群细节（一句：本仓库 Redis Token）。

**工作场景 ★**

| 场景 | 先做啥 |
|------|--------|
| 突然退出 | TTL/Redis/环境 |
| 部分接口 401 | 路由、白名单、头名 |
| 前端藏按钮但接口仍可调 | 后端必须鉴权 |

**过关：** 画出 Token 路径 + 401/403 分清 + 练习做过。

---

### P5 Nacos · 路由 · Feign ·

**和后面的联系：** 拆服务后的排错；日常不如 P0/P3/P4/P6 密，但面试爱问名词。

| 小课 | 内容 | 标 | 状态 |
|------|------|----|------|
| **P5.1** | 注册：服务名 + IP:port；控制台怎么看 | ★ | [ ] |
| **P5.2** | 网关路由再巩固（可与 P0.3 对照） | ★ | [ ] |
| **P5.3** | Feign：`name`=服务名；路径对齐 Controller | ★ | [ ] |
| **P5.4** | **排错练习：** 无实例 / 路径错 / Feign 路径错 | ★ | [ ] |
| **P5.x** | 配置中心深用、动态刷新 | ～ | 带过 |
| **P5.y** | Sentinel 限流熔断配置 | ～ | 名词即可 |

**练习（必做）**

1. Nacos 控制台确认 `ting-user` / `ting-gateway` 实例  
2. 看一遍 `UserFeignClient` 与对端 Controller 路径是否一致  
3. 故意停一个服务或改错 Feign 路径，看报错，再改回  

**面试 ·常考**

| 题 | 短答方向 |
|----|----------|
| Nacos 干什么？ | 注册发现（+可做配置中心） |
| `lb://`？ | 按服务名负载均衡 |
| Feign？ | 声明式服务间 HTTP 调用 |
| 限流/熔断？（～） | 防打爆/快失败，知概念 |

**工作场景 ·**

| 场景 | 先做啥 |
|------|--------|
| 503 / 无实例 | Nacos、服务名、进程是否存活 |
| Feign 失败 | 路径、返回结构、对端是否注册 |

**过关：** 练习 1～3 + 上表前 3 题短答。

---

### P6 前后端契约 ★

**和后面的联系：** 你是前端出身，这是日常最高频「吵架点」；和 P3 的 R/DTO、P4 的 Token 强绑定。

| 小课 | 内容 | 标 | 状态 |
|------|------|----|------|
| **P6.1** | JSON 字段与 TS 类型对齐 | ★ | [ ] |
| **P6.2** | HTTP 状态 vs `code`；拦截器判谁 | ★ | [ ] |
| **P6.3** | `X-Token`；401 清登录跳转 | ★ | [ ] |
| **P6.4** | CORS + OPTIONS 预检 | ★ | [ ] |
| **P6.5** | Apifox 环境 / 文档（springdoc 有则用） | ★ | [ ] |
| **P6.6** | **练习：** 字段名故意不一致再修好 | ★ | [ ] |

**练习（必做）**

1. 对照 `ting-web` 某 API 与后端 DTO 字段名  
2. 看前端 http 拦截器：如何处理 code≠0、401  
3. P6.6：改错一个字段名（或文档），复现问题后改回并记录  

**面试 ·常考**

| 题 | 短答方向 |
|----|----------|
| 跨域是什么？怎么解？ | 浏览器限制；网关/后端配 CORS |
| 为何 HTTP 200 仍可能失败？ | 看业务 code |
| OPTIONS？ | 跨域预检 |

**工作场景 ★**

| 场景 | 先做啥 |
|------|--------|
| 浏览器 CORS 红字 | 先查网关 CORS |
| Apifox 通前端不通 | 比 URL、头、代理、跨域 |
| then 里当成功页面当失败 | 查是否只判了 HTTP |

**过关：** P6.6 完成 + CORS 或双码短答。

---

### P7 口述 + 排障串讲 ★

**和前面的联系：** 把 P0～P6 收成「能讲、能查」，不新开冷门知识点。

| 小课 | 内容 | 标 | 状态 |
|------|------|----|------|
| **P7.1** | 5 分钟：一次请求从浏览器到 DB | ★ | [ ] |
| **P7.2** | 只过各块「面试 ·常考」表（弱项补） | ★ | [ ] |
| **P7.3** | 模拟排障：给现象说检查顺序 | ★ | [ ] |

**练习（必做）**

1. 不看文档讲一遍登录→带 Token→业务  
2. 抽 3 个现象（401 / CORS / 无实例）说检查顺序  

**综合快问（只保留高频）**

- 调用链路怎么走？  
- Token 存在哪？网关做了啥？  
- 为何用 DTO / 统一 R？  
- 分页怎么做？表结构变更怎么管？  
- 跨域怎么处理？  

～不考：JVM 调优、Seata、Sentinel 规则平台、Security 全套。

**过关：** 5 分钟口述 + 3 个排障顺序说得出。

---

## 篇章联系（一张图）

```text
P0 联调肌肉 ──┬── P4 鉴权 ──┬── P6 契约（你最熟的战场）
              │             │
              └── P5 注册/Feign（拆服务时才密）
              
P1 Java语法 ──→ P2 SQL/MP ──→ P3 独立写接口 ★核心产出

P7 = 把上面串成「能讲 + 能查」
```

---

## 不学清单（节约时间）

| 不学 | 原因 |
|------|------|
| JSP / Servlet 手写 / SSH | 过时 |
| Hibernate/JPA 与 MP 双线 | 先精 MP |
| Spring Security 全套死磕 | 先 Token + 网关/过滤器 + RBAC |
| Boot 自动配置源码精读 | 先会用 starter |
| 自写 Starter（可后置） | 非入门必需 |
| MapStruct 复杂映射专班 | C13 先手写/简单拷贝即可 |
| Seata / 分布式事务落地 | 知「尽量避免」即可 |
| Sentinel 复杂规则平台深配 | C29 只保留概念 |
| Dubbo 与 Feign 双线 | 先精 OpenFeign |
| K8s / Service Mesh | 后置 |
| 反射、JUC 大部、JVM 调优专班 | 后置 |
| Optional/Stream 深挖、集合八股对比 | P1～带过 |
| Nacos 配置动态刷新深配 | P5～带过 |
| JWT 长辩论、幂等深挖 | 概念一句即可 |

---

## J. Java 够用

### C01 类、方法、包

**①** 类 ≈ 模块/对象模板；方法 ≈ function；`package` ≈ 命名空间。`public` 先混眼熟。  
选读：廖雪峰 基本结构、包、方法。  
**②** 任意 `*Controller.java` / `*Service.java` 看 `package` 与 `class`、方法。  
**③** Controller 是不是一个 class？是/否  

### C02 String 与常用类型

**①** `String` 文本；`Long`/`Integer` 数字；比字符串内容用 `.equals` 不用 `==`。钱用 `BigDecimal`（综合课再见）。  
选读：字符串。  
**②** 看登录请求 DTO 字段类型。  
**③** 用户名常用类型？______（String）  

### C03 List / Map

**①** 多个对象 → `List<T>` ≈ 数组；按 id 查找 → `Map<K,V>` ≈ 前端 Map/对象字典。业务里这两样最高频。Set 去重用到再学。  
选读：[集合 List](https://liaoxuefeng.com/books/java/collection/list/index.html)、[Map](https://liaoxuefeng.com/books/java/collection/map/index.html)。  
**②** 看返回列表的接口（如 search）类型 `List<...>`。  
**③** 返回很多行数据，用 List 合适吗？是/否  

### C04 异常

**①** `throw` ≈ 前端 throw Error；业务失败常用自定义 `BizException`（或项目里同类名）。  
选读：异常简介。  
**②** 看 Service 里 `throw new BizException(...)`。  
**③** 本类项目密码错误，常见是 throw 还是只靠 return？______（throw）  

---

## M. 工程

### C05 Maven

**①** `pom.xml` 管依赖与版本；多模块 = 多个子项目共用父 POM。命令：`mvn package` / 指定模块运行。  
选读：[Maven 介绍](https://liaoxuefeng.com/books/java/maven/basic/index.html)。  
**②** 看任意项目根 `pom.xml` 的 `modules` / `dependencies`。  
**③** 第三方库主要靠 pom 声明吗？是/否  

---

## Q. MySQL

### C06 建表与 CRUD

**①** 主键、非空、唯一、注释；`SELECT/INSERT/UPDATE/DELETE`；`WHERE` + `ORDER BY` + `LIMIT`。  
**②** 看任意 `V1__*.sql` 或业务表结构。  
**③** 查多行要不要写 SELECT？是/否  

### C07 索引与事务（够用）

**①** 索引加快 WHERE/JOIN，别乱加；事务 = 多步同成同败，对应 `@Transactional`。隔离级别名字能听懂即可。  
**②** 看唯一索引、或 Service 上事务注解（有则看，无则记概念）。  
**③** 改表结构推荐版本化 SQL（Flyway/Liquibase）吗？是/否  

---

## B. Spring Boot 3（国内 Web 主力）

> Boot 3 主流；体感差异：`javax.*` → **`jakarta.*`**，JDK **17+**。

### C08 Boot3 是什么

**① 浅：** Boot = 快速做 Web：加 `starter` 就能用常用能力。  
**深一点：** `starter-web` / `validation` / `data-redis`；版本常由父 POM 管理；校验用 `jakarta.validation`。  
**②** 看父 POM 与 `starter-*`。  
**③** Boot3 校验是否更常见在 `jakarta.validation`？是/否  

### C09 启动、yml、分层

**①** `@SpringBootApplication`；yml 配端口/数据源/Redis；Controller → Service → Mapper。  
**②** 看启动类 + yml + 分层目录。  
**③** 业务逻辑更常放 Service 吗？是/否  

### C10 REST + 统一返回 R

**①** `@GetMapping`/`@PostMapping`、`@RequestBody`、`@PathVariable`、`@RequestParam`。  
国内通用：`{ code, message, data }`（`R`/`Result`）。成功 code 常见 `0` 或 `200`。  
**②** 看统一返回类 + 任一接口。  
**③** 统一返回是为了前端好解析吗？是/否  

### C11 HTTP 状态码 vs 业务 code

**① 浅：** 浏览器/网关先看 **HTTP 状态**（200/400/401/403/500）；body 里还有业务 **`code`**。  
**深一点（国内常见）：**  
- 很多项目 HTTP 也返回 200，用 body 的 `code≠0` 表示失败（前端 axios 要看 `data.code`）  
- 也有项目：校验失败 HTTP 400、未登录 401，同时 body 带 `R`  
- 联调时两边都要看，不要只看一个  

**②** 故意调一个失败接口，对比「HTTP 状态」和响应 JSON 里的 `code`。  
**③** 只看 HTTP=200，是否一定表示业务成功？是/否  

### C12 依赖注入

**①** 不要 `new Service()`；`@Service`/`@RestController` 注册；构造器注入（`final` + `@RequiredArgsConstructor`）。  
**②** 看 Controller/Service 注入字段。  
**③** 推荐业务里自己 new Mapper 吗？是/否  

### C13 DTO / 不直接暴露实体

**① 浅：** **DTO/VO** = 给接口用的数据形状；**Entity** = 对应数据库表。  
**深一点：**  
- 请求用 `LoginRequest`，响应用 `LoginResponse` / `UserProfile`，避免把带 `password` 的实体直接返回  
- 可用 `@JsonIgnore` 挡字段，但正规做法是「接口层用 DTO」  
- 对照前端：请求/响应 TS 类型，不等于数据库一行的全部字段  

**②** 对比 `SysUser`（实体）与 `LoginRequest`/`UserProfile`（DTO）；看密码字段如何被忽略。  
**③** 登录接口更适合直接返回数据库实体（含密码哈希）吗？是/否  

### C14 校验 + 全局异常

**①** `@NotBlank` + `@Valid`；`@RestControllerAdvice` + `@ExceptionHandler`；校验失败与业务异常分方法。  
**②** 看 DTO、Controller、GlobalExceptionHandler。  
**③** 没有 `@Valid` 时字段规则会自动跑吗？会/不会  

### C15 声明式事务

**①** 多步写库加 `@Transactional`；只读常不加；异常被吞可能不回滚。  
**②** 看多表写入的 Service（有则看）。  
**③** 事务注解更常加在 Service 吗？是/否  

### C16 多环境配置

**①** `application-dev.yml` / `prod`；`spring.profiles.active`；密钥不进仓库。  
**②** 看项目如何区分环境。  
**③** 生产密码适合写死在代码里吗？是/否  

### C17 OpenAPI / Apifox

**① 浅：** 用文档描述有哪些接口；**Apifox/Postman** 导入后可调通。  
**深一点：** Spring Boot 常用 **springdoc-openapi**（Boot3），浏览器打开 swagger-ui 或导出 `/v3/api-docs` 给 Apifox。联调前先有文档，少口口相传。  
**②** 若项目有 springdoc：打开 `/swagger-ui.html` 或导入 api-docs；无则记「要接文档工具」。  
**③** 前后端联调时，有接口文档会更省事吗？是/否  

---

## D. 数据访问

### C18 MyBatis-Plus

**①** `BaseMapper`；`LambdaQueryWrapper` 的 `eq`/`like`；`@TableName`。  
**②** 看 Mapper + Service 查询。  
**③** MP 能减少大量手写 SQL 吗？是/否  

### C19 Flyway

**①** 版本化改表；只新增 `Vn__xxx.sql`；禁止改已执行旧脚本。  
**②** 看 `db/migration`。  
**③** 能不能改已经执行过的 V1？能/不能  

---

## R. 登录态

### C20 Redis + Token

**①** 登录发 Token；Redis 存 token→用户/角色；请求头携带；多实例共享。  
**②** 看 login 写 Redis、过滤器校验。  
**③** Token 放 Redis 有利于多机共享吗？是/否  

### C21 BCrypt

**①** 禁止明文；BCrypt；`matches(明文, 密文)`。  
**②** 看 PasswordEncoder。  
**③** 库里应存明文密码吗？是/否  

---

## A. Spring Cloud Alibaba

> 国内常见：**Nacos + Gateway + Feign（+ Sentinel 概念）**。

### C22 SCA 全景与为何拆服务

**①** 拆服务有边界也有代价。认名字：Nacos、Gateway、Feign、Sentinel（概念）、Seata（知避免）。  
**②** 对照多模块：谁是网关、有哪些服务名。  
**③** 浏览器是否通常只打网关？是/否  

### C23 Nacos 注册发现

**①** `spring.application.name` + Nacos 地址；按服务名找实例。  
**②** 看 discovery 配置。  
**③** 注册的是实例（含地址端口）吗？是/否  

### C24 Nacos 配置中心（浅）

**①** 配置可放 Nacos 共享；很多项目仍用本地 yml——先懂「能集中管」。  
**②** 区分本地 yml vs Nacos config。  
**③** 必须把所有配置放 Nacos 才算微服务吗？是/否  

### C25 Gateway 路由

**①** `Path` + `uri: lb://服务名` + `StripPrefix`。  
**②** 看 gateway 路由 yml。  
**③** `lb://服务名` 是按服务名转发吗？是/否  

### C26 CORS 跨域

**① 浅：** 浏览器前端域名/端口与接口不一致时会跨域；服务端（常在**网关**）要配 CORS。  
**深一点：** `Access-Control-Allow-Origin` 等；预检 OPTIONS；凭证/Cookie 时不能随意 `*`。前后端分离管理端几乎必碰。  
**②** 看 gateway 的 `globalcors`（或后端 CORS 配置）。  
**③** 管理端域名和网关端口不同，是否经常需要配 CORS？是/否  

### C27 网关统一鉴权

**①** Filter 校验 Token；白名单放行登录；写入用户头给下游；勿信客户端伪造头。  
**②** 看鉴权 Filter。  
**③** 登录接口通常应在白名单吗？是/否  

### C28 OpenFeign

**①** `@FeignClient` 服务间调用；注意超时与失败。  
**②** 看 Feign 接口。  
**③** Feign 主要用于服务调服务吗？是/否  

### C29 Sentinel（概念）

**①** 限流、熔断；面试常问概念；复杂规则后置。  
**②** 口述即可。  
**③** 下游挂了还疯狂重试，容易更糟吗？是/否  

---

## F. 联调与验收

### C30 联调 + 小接口验收

**①** 契约（含 DTO 字段）、HTTP 与业务 code、鉴权头、CORS；排错看状态/body/日志/SQL/Redis；会用 Apifox。  
验收：带校验接口 + 统一返回 + 需登录（经网关）+ 能讲清链路。  
**②** 走通：登录 → Token → 业务；可选落地一小接口。  
**③** 联调是否应优先走网关？是/否  

---

## 推荐外部资料（少而精）

- Java / SQL：廖雪峰高频章  
- Spring Boot 3：官方 Getting Started（Web、Validation）  
- springdoc / Apifox：会导入 `/v3/api-docs` 即可  
- SCA：Nacos / Gateway / OpenFeign 入门文档（不做交易引擎长篇）  

---

## 给 AI 的带学指令

1. 目标是国内通用高频技能，不是堆仓库功能、不是八股全书。  
2. 按 LEARNING 图例：**★必练讲透+练习**；**·常考短答**；**～带过一句**，不展开。  
3. 每小课：①是什么 → ②为什么 → ③细节 → ④**练习必做** → ⑤复盘 → ⑥仅高频面试 → ⑦仅常见工作场景。  
4. 几乎零基础；前端可对照；不用生活比喻；题难直接给答案+原因。  
5. 当前按 **P0.1→…**；不过关不跳；更新勾选与 `CONTEXT.md` 当前小课号。
